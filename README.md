# IR-Remote-Controlled-Binary-Decimal-Counter
This project implements an IR remote–controlled digital counter using an Arduino, displaying the count in binary (LEDs + LCD) and decimal (7-segment via CD4511).
